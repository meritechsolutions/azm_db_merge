* check sqlite3 ver on both existing win and ubuntu - check min ver
   and raise if not... debian case 3.7 didnt work with multi cmd
   required to use

* sqlite3 merge
  * create if not exists
  * alter
  * insert specify cols

* check for sqlite3 binary and ABORT at start if failed...

* import to postgres
  * test alter add multiple col

* hook gen legacy tables for cur php/mysql session etc tables?

 
